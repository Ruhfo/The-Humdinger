package com.humdinger.humdinger;

import android.graphics.Bitmap;

public class TapButton extends Button {

    public TapButton(float x, float y, Bitmap normalBitmap, Bitmap pressedBitmap, String message) {
        super(x, y, normalBitmap, pressedBitmap, message);
    }
}
